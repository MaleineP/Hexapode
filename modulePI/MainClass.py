from modulePI.Connexion import Connexion
from modulePI.Initialize import Initialize
from modulePI.Light import Light
from modulePI.Move import Move
from modulePI.Rotate import Rotate
from modulePI.Stop import Stop
from modulePI.Temperature import Temperature


class MainClass:

    @staticmethod
    def main():
        # Initialize.start_initialisation()
        # connexion_to_pc = Connexion().get_instance()
        # connexion_to_pc.run()
        # Stop.clear_pos()
        # Rotate.rotate_left()
        # Rotate.rotate_right()
        # Move.walk_forward()
        # Move.walk_backward()
        # Light.green_blink()
        # Light.turn_on_yellow()
        # Light.turn_off_yellow()
        # Temperature.get_temperature()
